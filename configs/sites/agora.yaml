# Agora - Fast scraper
# Platform: PrestaShop with vertical mega-menu, /fr/ prefix
base_url: "https://agora.tn/fr"

settings:
  max_retries: 3
  retry_delay: 2
  max_consecutive_failures: 10
  request_timeout: 30
  page_timeout: 30000
  wait_after_load: 1
